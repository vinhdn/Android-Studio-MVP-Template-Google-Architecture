<template format="4"
    revision="1"
    name="MVP Template Biin Android"
    description="Creates a new MVP classes - Presenter, View and Contractor between the two">

    <category value="MVP" />

    <parameter id="moduleName"
    name="Feature Name"
    type="string"
    constraints="class|unique|nonempty"
    default="Feature"
    help="The name of the functionality that requires MVP views"/>

    <parameter
    						id="includeActivity"
    						name="Create Activity?"
    						type="boolean"
    						default="false"
    						help="Generate a Activity for the fragment" />

    <parameter
    						id="isKotlin"
    						name="use Kotlin?"
    						type="boolean"
    						default="true"
    						help="Generate files write by Kotlin" />

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />
</template>
